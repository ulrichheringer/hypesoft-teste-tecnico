apiVersion: 1

providers:
  - name: "Hypesoft Dashboards"
    orgId: 1
    folder: "Hypesoft"
    folderUid: "hypesoft"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
